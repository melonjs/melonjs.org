---
import Header from '../components/header.astro'
import '../styles/variables.css'
import '../styles/global.css'

const siteName = 'melonJS'
const siteUrl = 'https://melonjs.org'
const defaultDescription =
    'melonJS is a free, open-source HTML5 game engine for building 2D games with JavaScript and TypeScript. Fully featured, fast, and dependency-free.'
const defaultTitle =
    'melonJS | A Modern & Fully Featured HTML5 Game Engine'
const ogImage = `${siteUrl}/img/banner.png`

const {
    description = defaultDescription,
    title = defaultTitle,
    image = ogImage,
} = Astro.props

const canonicalUrl = new URL(Astro.url.pathname, siteUrl).href

const ogImageAlt = 'melonJS: a modern & fully featured HTML5 game engine'

const jsonLd = {
    '@context': 'https://schema.org',
    '@type': 'SoftwareApplication',
    name: 'melonJS',
    description: defaultDescription,
    url: siteUrl,
    applicationCategory: 'Game Engine',
    operatingSystem: 'Web Browser',
    offers: {
        '@type': 'Offer',
        price: '0',
        priceCurrency: 'USD',
    },
    license: 'https://opensource.org/licenses/MIT',
    author: {
        '@type': 'Organization',
        name: 'AltByte Pte Ltd',
        url: 'https://www.altbyte.com',
    },
    programmingLanguage: ['JavaScript', 'TypeScript'],
    sameAs: [
        'https://github.com/melonjs/melonJS',
        'https://www.npmjs.com/package/melonjs',
        'https://discord.gg/aur7JMk',
    ],
}
---

<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />

        <title>{title}</title>
        <meta name="description" content={description} />
        <meta name="keywords" content="HTML5 game engine, JavaScript game engine, TypeScript game engine, 2D game engine, WebGL game engine, melonJS, open source game engine, browser game engine, Tiled map editor, indie game development" />
        <meta name="author" content="AltByte Pte Ltd" />
        <meta name="robots" content="index,follow,max-image-preview:large" />
        <meta name="theme-color" content="#202020" />
        <link rel="canonical" href={canonicalUrl} />

        <!-- Open Graph -->
        <meta property="og:type" content="website" />
        <meta property="og:url" content={canonicalUrl} />
        <meta property="og:title" content={title} />
        <meta property="og:description" content={description} />
        <meta property="og:image" content={image} />
        <meta property="og:image:width" content="970" />
        <meta property="og:image:height" content="297" />
        <meta property="og:image:alt" content={ogImageAlt} />
        <meta property="og:site_name" content={siteName} />
        <meta property="og:locale" content="en_US" />

        <!-- Twitter Card -->
        <meta name="twitter:card" content="summary_large_image" />
        <meta name="twitter:title" content={title} />
        <meta name="twitter:description" content={description} />
        <meta name="twitter:image" content={image} />
        <meta name="twitter:image:alt" content={ogImageAlt} />

        <!-- Favicon -->
        <link rel="icon" href="/img/favicon.svg" />
        <link rel="apple-touch-icon" href="/img/banner.png" />

        <!-- Preconnect to external origins -->
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            rel="stylesheet"
            href="https://fonts.googleapis.com/css2?family=Droid+Sans&display=swap"
        />

        <!-- Structured Data -->
        <script type="application/ld+json" set:html={JSON.stringify(jsonLd)} />
    </head>
    <body>
        <Header />
        <slot />
    </body>
</html>
